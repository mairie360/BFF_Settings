name: BFF Settings CICD

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/BFFs-cicd.yml@v1.15.1
    with:
      package_name: bff-settings
      node_version: "22"
      cicd_version: v1.14.0
    secrets: inherit
